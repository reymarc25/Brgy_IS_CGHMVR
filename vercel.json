{
  "version": 2,
  "buildCommand": "composer install --no-dev --prefer-dist --optimize-autoloader && php artisan package:discover --ansi && npm run build",
  "builds": [
    {
      "src": "public/index.php",
      "use": "vercel-php@0.7.3"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/public/$1",
      "check": true
    },
    {
      "src": "/(.*)",
      "dest": "/public/index.php"
    }
  ]
}
